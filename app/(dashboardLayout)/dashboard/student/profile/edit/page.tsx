import { StudentProfileUpdateForm } from '@/components/modules/authentication/Update-stduent-form'
import { StudentProfile } from '@/types/student.type'
import React from 'react'

export default function UserProfileEdit({children}: {children: React.ReactNode}) {

  return (
    <div>
        {children}
    </div>
  )
}
