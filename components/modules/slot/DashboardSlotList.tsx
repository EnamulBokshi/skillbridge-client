import { ISlotResponse } from '@/types/slot.type'
import React from 'react'

export default function DashboardSlotList({role, slots, isEditable}: {role: "TUTOR" | "STUDENT", slots: ISlotResponse[], isEditable?: boolean}) {
    
  return (
    <div>DashboardSlotList</div>
  )
}
