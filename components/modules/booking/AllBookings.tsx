import { USER_ROLES } from '@/constants'
import React from 'react'

export default function AllBookings({userRole}: {userRole: USER_ROLES}) {
    let bookings = [];
    if(userRole === USER_ROLES.STUDENT){
    }
  return (
    <div>
        
    </div>
  )
}
